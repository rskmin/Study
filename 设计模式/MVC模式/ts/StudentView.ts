export default class StudentView {
  printStudentDetails(studentName: string, studentRollNo: string): void {
    console.log(
      `
    Student:
    Name: ${studentName}
    Roll No: ${studentName}
    `
    )
  }
}
